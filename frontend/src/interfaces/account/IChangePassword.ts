export type IChangePassword = {
    oldPassword: string,
    newPassword: string
}

export type IChangePasswordInput = {
    oldPassword: string,
    newPassword: string,
    repeatedNewPassword: string
}